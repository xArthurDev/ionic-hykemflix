const arr = [10, 199, 23];

const ToHex = (arr) => {
  const result = arr.map((num) => {
    const toHex = num.toString(16).toUpperCase();
    return toHex.length < 2 ? `0${toHex}` : toHex;
  }).join('');
  console.log(result);
  return result === '0AC717';

  // TIRA


  // ESSAS


  // VARIAVEIS


  // ARTHUR vou pensar

  if(pensar) return 'vai durar até amanhã'
  if(!pensar) return 'vai tentar'
  if(pensar === undefined) return 'tela azul'

  return abraços
};

// retornar o valor do array (RGB) em Hexadecimal

console.log(ToHex(arr));


// ------------------------------

const origin = [2, 1, 4, 3, 2, 4, 1, 2, 3, 5, 12, 35, 6, 345, 435, 3, 12312, 4532, 536, 7]
const order = [4, 12, 2, 435]

const orderBy = (origin, order) => {
  // Escreva uma função que ordene o array origin na ordem do array order, incluindo os valores duplicados e ignorando
  // os itens que não pertencem ao array order. 
  return
}

// console.log(orderBy(origin, order))
// output
